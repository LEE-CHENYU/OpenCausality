{
  "mcpServers": {
    "opencausality-query": {
      "command": "python",
      "args": ["-m", "shared.mcp.query_server"],
      "cwd": "${CLAUDE_PROJECT_ROOT}",
      "env": {
        "PYTHONPATH": "${CLAUDE_PROJECT_ROOT}"
      }
    }
  }
}
